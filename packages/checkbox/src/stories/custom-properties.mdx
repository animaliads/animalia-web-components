import types from './img/checkbox-types.jpg';

## Propriedades customizáveis

O componente possui propriedades customizáveis, que possibilitam fazer alterações, de acordo com a marca ou o tema a ser utilizado. Com isso, garantimos uma consistência ao Design System, sem perder a identidade de cada marca.

O checkbox possui quadro estados, que se modificam de acordo com a interação do usuário, sendo eles: _Normal_, _Hover_, _Focused_ e _Disabled_.

<img
  src={types}
  alt="Os estados de interação do checkbox unchecked, com as respectivas versões checked e mixed abaixo"
/>
